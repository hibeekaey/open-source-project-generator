<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Server Error - {{.Name}}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: linear-gradient(135deg, #fd79a8 0%, #e84393 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #333;
        }
        
        .error-container {
            background: white;
            border-radius: 12px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            padding: 3rem;
            text-align: center;
            max-width: 500px;
            width: 90%;
        }
        
        .error-icon {
            font-size: 5rem;
            margin-bottom: 1rem;
        }
        
        .error-code {
            font-size: 4rem;
            font-weight: 700;
            color: #e84393;
            margin-bottom: 1rem;
        }
        
        .error-title {
            font-size: 1.5rem;
            font-weight: 600;
            color: #2d3436;
            margin-bottom: 1rem;
        }
        
        .error-message {
            font-size: 1rem;
            color: #636e72;
            line-height: 1.6;
            margin-bottom: 2rem;
        }
        
        .error-actions {
            display: flex;
            gap: 1rem;
            justify-content: center;
            flex-wrap: wrap;
            margin-bottom: 2rem;
        }
        
        .btn {
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 6px;
            font-size: 0.9rem;
            font-weight: 500;
            text-decoration: none;
            cursor: pointer;
            transition: all 0.3s ease;
            display: inline-block;
        }
        
        .btn-primary {
            background: #e84393;
            color: white;
        }
        
        .btn-primary:hover {
            background: #d63384;
            transform: translateY(-2px);
        }
        
        .btn-secondary {
            background: #fd79a8;
            color: white;
        }
        
        .btn-secondary:hover {
            background: #fc5c9c;
            transform: translateY(-2px);
        }
        
        .retry-btn {
            background: #00b894;
            color: white;
        }
        
        .retry-btn:hover {
            background: #00a085;
            transform: translateY(-2px);
        }
        
        .status-indicator {
            display: inline-block;
            width: 12px;
            height: 12px;
            border-radius: 50%;
            margin-right: 0.5rem;
        }
        
        .status-checking {
            background: #fdcb6e;
            animation: pulse 2s infinite;
        }
        
        .status-ok {
            background: #00b894;
        }
        
        .status-error {
            background: #e17055;
        }
        
        @keyframes pulse {
            0% { opacity: 1; }
            50% { opacity: 0.5; }
            100% { opacity: 1; }
        }
        
        .error-details {
            margin-top: 2rem;
            padding-top: 2rem;
            border-top: 1px solid #ecf0f1;
            font-size: 0.8rem;
            color: #95a5a6;
        }
        
        .request-id {
            font-family: 'Courier New', monospace;
            background: #f8f9fa;
            padding: 0.25rem 0.5rem;
            border-radius: 4px;
            margin: 0.5rem 0;
        }
        
        .support-info {
            background: #f8f9fa;
            border-radius: 6px;
            padding: 1rem;
            margin-top: 1rem;
            font-size: 0.9rem;
        }
        
        @media (max-width: 480px) {
            .error-container {
                padding: 2rem 1.5rem;
            }
            
            .error-code {
                font-size: 3rem;
            }
            
            .error-title {
                font-size: 1.25rem;
            }
            
            .error-actions {
                flex-direction: column;
            }
        }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="error-icon">⚠️</div>
        <div class="error-code">500</div>
        <h1 class="error-title">Server Error</h1>
        <p class="error-message">
            We're experiencing technical difficulties. Our team has been notified and is working to resolve the issue.
        </p>
        
        <div class="error-actions">
            <button onclick="location.reload()" class="btn retry-btn">Try Again</button>
            <a href="/" class="btn btn-primary">Go Home</a>
            <button onclick="history.back()" class="btn btn-secondary">Go Back</button>
        </div>
        
        <div class="support-info">
            <div style="margin-bottom: 0.5rem;">
                <span class="status-indicator status-checking" id="statusIndicator"></span>
                <span id="statusText">Checking server status...</span>
            </div>
            <div style="font-size: 0.8rem; color: #636e72;">
                If the problem persists, please contact our support team.
            </div>
        </div>
        
        {{if .RequestID}}
        <div class="error-details">
            <div>Request ID: <span class="request-id">{{.RequestID}}</span></div>
            <div>Time: {{.Timestamp}}</div>
            <div>Please provide this information when contacting support.</div>
        </div>
        {{end}}
    </div>
    
    <script>
        // Security: Prevent clickjacking
        if (top !== self) {
            top.location = self.location;
        }
        
        // Security: Clear any sensitive data from browser history
        if (window.history && window.history.replaceState) {
            window.history.replaceState(null, null, window.location.href);
        }
        
        // Simple status check simulation
        setTimeout(function() {
            const indicator = document.getElementById('statusIndicator');
            const text = document.getElementById('statusText');
            
            // Simulate a status check (in real implementation, this would be an actual health check)
            const isHealthy = Math.random() > 0.3; // 70% chance of being healthy
            
            if (isHealthy) {
                indicator.className = 'status-indicator status-ok';
                text.textContent = 'Server is responding normally. Please try again.';
            } else {
                indicator.className = 'status-indicator status-error';
                text.textContent = 'Server is still experiencing issues. Please try again later.';
            }
        }, 3000);
    </script>
</body>
</html>
</content>
</invoke>